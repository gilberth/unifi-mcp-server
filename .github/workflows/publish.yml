name: Build and Publish to NPM

on:
  push:
    branches: [ main ]
    paths-ignore:
      - 'README.md'
      - 'CHANGELOG.md'
      - '.gitignore'
      - 'LICENSE'
  pull_request:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest
    
    strategy:
      matrix:
        node-version: [18.x, 20.x]
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Setup Node.js ${{ matrix.node-version }}
      uses: actions/setup-node@v4
      with:
        node-version: ${{ matrix.node-version }}
        cache: 'npm'
        
    - name: Install dependencies
      run: npm ci
      
    - name: Run TypeScript compilation
      run: npm run build
      
    - name: Run tests (if available)
      run: npm test
      
    - name: Validate MCP server
      run: |
        echo "🔍 Validating MCP server..."
        # Verificar que el archivo principal existe
        if [ -f "dist/index.js" ]; then
          echo "✅ Main file exists: dist/index.js"
        else
          echo "❌ Main file not found: dist/index.js"
          exit 1
        fi
        
        # Verificar que el archivo tiene contenido válido
        if [ -s "dist/index.js" ]; then
          echo "✅ Main file has content"
        else
          echo "❌ Main file is empty"
          exit 1
        fi
        
        # Verificar sintaxis básica de JavaScript
        node -c dist/index.js
        echo "✅ JavaScript syntax is valid"
        
        echo "✅ MCP server validation completed successfully"

  publish:
    needs: test
    runs-on: ubuntu-latest
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '20.x'
        registry-url: 'https://registry.npmjs.org'
        cache: 'npm'
        
    - name: Install dependencies
      run: npm ci
      
    - name: Build project
      run: npm run build
      
    - name: Check if version changed
      id: version-check
      run: |
        # Obtener la versión actual del package.json
        CURRENT_VERSION=$(node -p "require('./package.json').version")
        echo "current_version=$CURRENT_VERSION" >> $GITHUB_OUTPUT
        
        # Verificar si esta versión ya existe en npm
        if npm view unifi-mcp-server@$CURRENT_VERSION version 2>/dev/null; then
          echo "version_exists=true" >> $GITHUB_OUTPUT
          echo "Version $CURRENT_VERSION already exists in npm"
        else
          echo "version_exists=false" >> $GITHUB_OUTPUT
          echo "Version $CURRENT_VERSION is new, will publish"
        fi
        
    - name: Check NPM Token availability
      id: npm-token-check
      run: |
        if [ -z "${{ secrets.NPM_TOKEN }}" ]; then
          echo "npm_token_available=false" >> $GITHUB_OUTPUT
          echo "⚠️ NPM_TOKEN not configured - skipping publication"
        else
          echo "npm_token_available=true" >> $GITHUB_OUTPUT
          echo "✅ NPM_TOKEN available - publication enabled"
        fi
        
    - name: Publish to npm
      if: steps.version-check.outputs.version_exists == 'false' && steps.npm-token-check.outputs.npm_token_available == 'true'
      run: npm publish
      env:
        NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
        
    - name: Skip publish (no NPM token)
      if: steps.version-check.outputs.version_exists == 'false' && steps.npm-token-check.outputs.npm_token_available == 'false'
      run: |
        echo "⚠️ NPM publication skipped - NPM_TOKEN not configured"
        echo "💡 To enable automatic publication:"
        echo "   1. Generate an NPM token at https://www.npmjs.com/settings/tokens"
        echo "   2. Add it as NPM_TOKEN secret in GitHub repository settings"
        echo "   3. Re-run the workflow or push a new commit"
        
    - name: Create GitHub Release
      if: steps.version-check.outputs.version_exists == 'false' && steps.npm-token-check.outputs.npm_token_available == 'true'
      uses: softprops/action-gh-release@v1
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      with:
        tag_name: v${{ steps.version-check.outputs.current_version }}
        name: Release v${{ steps.version-check.outputs.current_version }}
        body: |
          ## UniFi MCP Server v${{ steps.version-check.outputs.current_version }}
          
          ### 🚀 Nuevas Funcionalidades
          - Gestión avanzada de QoS (Quality of Service)
          - Administración de clientes VPN
          - Configuración de Port Forwarding
          - Gestión de rutas de tráfico
          - Políticas avanzadas de Firewall
          
          ### 📦 Instalación
          ```bash
          npm install -g unifi-mcp-server@${{ steps.version-check.outputs.current_version }}
          ```
          
          ### 📋 Herramientas Disponibles
          - **QoS:** 2 herramientas
          - **VPN:** 2 herramientas  
          - **Port Forwarding:** 2 herramientas
          - **Traffic Routes:** 2 herramientas
          - **Advanced Firewall:** 2 herramientas
          
          **Total:** 24 herramientas para gestión completa de redes UniFi
          
          Para más información, consulta la [documentación completa](https://github.com/gilberth/unifi-mcp-server/blob/main/README.md).
        draft: false
        prerelease: false
        
    - name: Skip publish (version exists)
      if: steps.version-check.outputs.version_exists == 'true'
      run: |
        echo "⚠️ Version ${{ steps.version-check.outputs.current_version }} already exists in npm"
        echo "💡 To publish a new version, update the version in package.json"
        echo "   Example: npm version patch|minor|major"
        
    - name: Skip GitHub Release (no NPM token)
      if: steps.version-check.outputs.version_exists == 'false' && steps.npm-token-check.outputs.npm_token_available == 'false'
      run: |
        echo "⚠️ GitHub Release skipped - NPM_TOKEN not configured"
        echo "💡 GitHub Release will be created automatically when NPM publication succeeds"